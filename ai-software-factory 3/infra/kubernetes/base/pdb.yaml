apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: factory-simulator
  namespace: ai-factory
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: factory-simulator

