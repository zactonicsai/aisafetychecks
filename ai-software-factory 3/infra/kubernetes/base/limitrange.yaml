apiVersion: v1
kind: LimitRange
metadata:
  name: default-container-limits
  namespace: ai-factory
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 50m
        memory: 64Mi
      default:
        cpu: 500m
        memory: 256Mi

