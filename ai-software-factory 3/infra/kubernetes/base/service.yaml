apiVersion: v1
kind: Service
metadata:
  name: factory-simulator
  namespace: ai-factory
  labels:
    app.kubernetes.io/name: factory-simulator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: factory-simulator
  ports:
    - name: http
      port: 80
      targetPort: http

