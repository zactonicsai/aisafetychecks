name: ai-software-factory
services:
  simulator:
    build:
      context: .
      dockerfile: Containerfile
    image: ai-software-factory-simulator:local
    ports:
      - "127.0.0.1:${FACTORY_PORT:-8080}:8080"
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped

