apiVersion: v1
kind: Service
metadata:
  name: hello-web
  namespace: hands-on
spec:
  type: NodePort
  selector:
    app: hello-web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
